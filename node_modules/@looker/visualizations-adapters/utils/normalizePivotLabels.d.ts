import type { Pivots } from '../types';
export declare const normalizePivotLabels: (pivots: Pivots) => {
    label: string;
    data: import("../types").SDKRecord;
    key: string;
    is_total: boolean;
}[];
